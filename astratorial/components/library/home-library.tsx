"use client";
import Image from "next/image";
import Link from "next/link";
import { useEffect, useState } from "react";
import type { Tutorial } from "@/lib/contracts";
import { tutorialHref } from "@/lib/contracts";
import { examples } from "@/lib/examples";
import { api, useAppConfig } from "@/lib/client";
import { BrandMark, Icon } from "@/components/shell/icon";
import { categoryIcons, categoryLabels, TutorialCard } from "./tutorial-card";

export function HomeLibrary() {
  const { config } = useAppConfig();
  const [query, setQuery] = useState("");
  const [category, setCategory] = useState("all");
  const [mine, setMine] = useState<Tutorial[]>([]);
  useEffect(() => { if (!config?.user || !config.services.database) return; let active = true; api<{ tutorials: Tutorial[] }>("/api/tutorials?scope=mine").then((data) => { if (active) setMine(data.tutorials); }).catch(() => {}); return () => { active = false; }; }, [config?.user, config?.services.database]);
  const filtered = examples.filter((tutorial) => (category === "all" || tutorial.category === category) && `${tutorial.title} ${tutorial.description} ${tutorial.category}`.toLowerCase().includes(query.toLowerCase()));
  return <div className="home-page">
    <section className="home-hero"><div className="hero-copy"><span className="eyebrow"><span className="tiny-spark">✳</span> BIG LITTLE POSSIBILITIES</span><h1>A little guidance.<br />A lot more<br /><em>possible.</em></h1><p>From your first espresso to your next home project. Turn the things around you into things you can do.</p><Link href="/create" className="button button-primary hero-cta"><Icon name="plus" size={18} />Create your first tutorial<Icon name="arrow" size={17} /></Link><div className="hero-footnote"><Icon name="camera" size={15} /><span>Your space. Your pace. Your personal guide.</span></div></div><div className="hero-visual"><Image src="/images/hero.png" alt="A sunlit kitchen with an espresso machine and a cup ready for the morning" fill sizes="(max-width: 760px) 100vw, 52vw" priority /><div className="hero-image-tint" /><span className="hero-scene-label"><span className="status-dot" /> A NEW WAY TO LEARN</span><Link href={tutorialHref(examples[0])} className="hero-scene-card"><div className="hero-scene-icon"><Icon name="play" size={18} /></div><div><span>TAKE A LOOK AROUND</span><strong>Your everyday espresso</strong><small>5 minutes to a better morning</small></div><Icon name="arrow" size={18} /></Link><span className="hero-image-caption">INSPIRATION, RIGHT WHERE YOU ARE.</span></div></section>
    <section className="how-it-works" aria-label="How astratorial works"><div><span className="how-number">01</span><span className="how-icon"><Icon name="camera" /></span><div><strong>Show us your space</strong><p>A quick guided scan. A little context.</p></div></div><span className="how-divider" /><div><span className="how-number">02</span><span className="how-icon"><Icon name="box" /></span><div><strong>See it come together</strong><p>Explore your personal 3D walkthrough.</p></div></div><span className="how-divider" /><div><span className="how-number">03</span><span className="how-icon"><Icon name="sparkles" /></span><div><strong>Now, it&apos;s your turn</strong><p>A voice and camera guide by your side.</p></div></div></section>
    {mine.length > 0 && <section className="home-section"><div className="section-heading"><div><span className="eyebrow">PICK UP WHERE YOU LEFT OFF</span><h2>From your space</h2></div><Link href="/library" className="text-link">My tutorials <Icon name="arrow" size={16} /></Link></div><div className="tutorial-grid">{mine.slice(0, 3).map((tutorial) => <TutorialCard key={tutorial.id} tutorial={tutorial} />)}</div></section>}
    <section className="home-section"><div className="section-heading"><div><span className="eyebrow">A GOOD PLACE TO START</span><h2>What will you try today?</h2></div><Link href="/explore" className="text-link">Explore tutorials<Icon name="arrow" size={16} /></Link></div><div className="library-tools"><div className="category-filters" aria-label="Filter tutorials by category"><button className={`filter-chip ${category === "all" ? "selected" : ""}`} onClick={() => setCategory("all")}>A little of everything</button>{(["coffee", "cooking", "assembly"] as const).map((key) => <button className={`filter-chip ${category === key ? "selected" : ""}`} key={key} onClick={() => setCategory(key)}><Icon name={categoryIcons[key]} size={15} />{categoryLabels[key]}</button>)}</div><label className="search-field"><Icon name="search" size={17} /><input aria-label="Search example tutorials" placeholder="Find a little inspiration" value={query} onChange={(event) => setQuery(event.target.value)} />{query && <button aria-label="Clear search" onClick={() => setQuery("")}><Icon name="close" size={15} /></button>}</label></div><div className="tutorial-grid">{filtered.map((tutorial) => <TutorialCard tutorial={tutorial} key={tutorial.id} />)}</div>{filtered.length === 0 && <div className="empty-state compact"><Icon name="search" size={28} /><h3>A fresh start?</h3><p>No examples match that search yet.</p><button className="button button-secondary" onClick={() => { setQuery(""); setCategory("all"); }}>Show all examples</button></div>}</section>
    <section className="possibility-banner"><div className="banner-mark"><BrandMark size={42} /></div><div><h2>That thing you&apos;ve been meaning to try?</h2><p>Let&apos;s figure it out, right where you are.</p></div><Link href="/create" className="button button-secondary">Make it happen <Icon name="arrow" size={17} /></Link></section>
  </div>;
}
